apiVersion: v1
kind: Service
metadata:
  name: email-service
  namespace: backend
  labels:
    app.kubernetes.io/name: email-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: email-service
  ports:
    - name: http
      protocol: TCP
      port: 3005
      targetPort: http
