apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: backend
  labels:
    app.kubernetes.io/name: user-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: user-service
  ports:
    - name: http
      protocol: TCP
      port: 3002
      targetPort: http
