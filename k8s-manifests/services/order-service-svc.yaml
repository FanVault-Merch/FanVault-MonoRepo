apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: backend
  labels:
    app.kubernetes.io/name: order-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: order-service
  ports:
    - name: http
      protocol: TCP
      port: 3004
      targetPort: http
