apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: backend
  labels:
    app.kubernetes.io/name: auth-service
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: auth-service
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: http
